# 📅 2026-06-19 工作日志

> 协作：小彩（管家） · C哥（代码） · 小宇（负责人）
> 工作副本：`C:\tmp\logistics-route-command-center-layout-dashboard-shell`
> 主线：夯实地基 → 深挖数据 → 企业级优化（4 个 commit）

---

## ✅ 阶段 1：文档与记忆对齐（a8e3e02）

- `backend/.env.example` / `.env.production.example` / `docs/数据库设计.md` 对齐 PostgreSQL/PostGIS
- 新建 `.shared-memory/PROGRESS.md` / `CONTEXT.md` / `DECISIONS.md`（三件套）
- 更新 `TASKS.md` / `MEMORIES.md` / 今日 daily
- **关键洞察**：数据库双轨机制（本地 .env 给连接串；生产分项变量由 compose 拼接注入 Flask）

---

## ✅ 阶段 2：代码清理 + 配置化（1ed6a83）

- `config.py` 第 33/81 行重复 `.replace('\\','/')` 清理
- `analytics_service.py` 抽取 `COST_RATIO` 常量（`LOGISTICS_COST_RATIO` 可配），修复 L70/L167 既有不一致
- 完成 CODE_REVIEW 低优 #8（配置化成本计算）

---

## ✅ 阶段 3：烟雾测试诊断 + 正式化（3cdcdfe）

- 深挖发现：线上 dispatch **健康无 bug**，是 3 月老脚本 `_tmp_dispatch_smoke.py` 与 6 月重构后接口结构不匹配（health 的 orders/vehicles→order_sources/vehicle_source；preview 的 solver 是字符串非 dict）
- 正式化为 `scripts/smoke_test_dispatch.py`（环境变量 + 健壮性），删除旧脚本

---

## ✅ 阶段 4：数据真实性大补全（本地主库）

### 深挖诊断（连本地 PG 只读审计）
- 数据来源真实：`物流运输数据集.csv`（5 万）+ `数据.xlsx`（330 行业面板）
- **核心问题**：76% 运单坐标缺失（geo_status resolved 仅 24%）
- **根因**：37% origin_city_std 为 NULL，且 11 个虚构地名（凤英县/倩县/红梅市…）
- **双重验证**：area_format 行政区划名录（29508 条）+ Grep 交叉确认 —— 18 真实城市全 YES，11 虚构全 NO

### 修复（高德 geocoding 真实坐标，dryrun 验证→apply 写库）
- Step1：4 个真实城市（乐清/成县/秀英区/澳门）补坐标
- Step2：11 虚构地名 → 真实城市（义乌/昆山/晋江/临沂/武汉/长沙/东莞/苏州/南昌/贵阳/昆明）
- **结果**：resolved 24%→**100%**，坐标缺失 76%→**0%**，城市覆盖 18→**29**

---

## ✅ 阶段 5：dispatch 性能优化（4b05c82）

### 实测发现
- UPDATE 后 authenticity B-→B，但 solve_time **188 秒**（精确距离极慢）

### 根因
- `_build_order_distance_lookup` 建 40×40=**1600 对全矩阵**，但 dispatch 只用 20 对（origin→destination）
- 1580 对废计算 + 跨 order 废对大量 IMPLAUSIBLE → 逐对 driving_route 兜底 → 雪崩

### 优化 + 修复
- 逐对查询（1600→20 对）→ solve_time 188s→**17.83ms**
- `distance_cache_service` 新增 `_call_amap_with_route_fallback`（get_distance 加 driving_route 兜底）
- source 规范化（cache+is_exact→cache_exact）修复 authenticity 误判
- **结果**：17.83ms + authenticity 稳定 B + 无 IMPLAUSIBLE

---

## 📌 经验沉淀（详见 MEMORIES.md）

1. 改配置/数据前先顺链路核实（不凭印象）
2. 数据真实性要交叉验证（area_format 名录验证虚构地名）
3. 性能优化找根因（1600 废对 vs 20 真需对）
4. 优化要验证不回退（性能↑但 authenticity 曾掉档，二次修复）

---

## ⏭️ 待办

- Node/Route 坐标审查（独立表）
- 部署线上（4 commit + 本地数据，待小宇确认）
- CODE_REVIEW 剩余 2 低优（ACO 信息素 / SA 冷却）
- 临时脚本清理（进行中）

---

_记录人：C哥 · 2026-06-19_
